.modal-dialog.auth-modal

  .modal-header
    a.btn-close ng-click="toggleLogin()" ×
    h4 Login in to you account

  .modal-body
    form name="form"
      .form-group
        label Email
        input.form-control(
          type="email"
          name="email"
          ng-model="credentials.email"
          required=""
          ng-enter="submit(form)"
          placeholder="Email"
          ng-class="{error: form.email.$error.required && form.$submitted}")

      .form-group
        label Password
        input.form-control(
          type="password"
          name="password"
          ng-model="credentials.password"
          required=""
          ng-enter="login(form)"
          placeholder="Password"
          ng-class="{error: form.password.$error.required && form.$submitted}")


  .modal-footer
    .row
      .col-md-8
        a.left-label ng-click="toggleModal('register')" Not a Member yet?
      .col-md-4
        a.btn.btn-primary.btn-block ng-click="login(form)" ng-class="{'disable': disabledSubmit}" Login
